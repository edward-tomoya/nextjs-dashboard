import { Inter, Lusitana } from 'next/font/google'

export const inter = Inter({ display: 'swap', subsets: ['latin'] });
export const lusitana = Lusitana({ display: 'swap', subsets: ['latin'], weight: ['400', '700']})
